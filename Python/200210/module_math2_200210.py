# 해당 모듈에서 필요한 함수만 가져온다
# 변수명 지을 때 중복 피하기 위함
from math import floor

# 해당 모듈 모든 함수. 함수 앞에 math를 안 붙여도 됨
from math import *  

print("floor(2.4) :", floor(2.75)) # 내림
print("ceil(2.4) :", ceil(2.75)) # 올림

